# Entorno local (claves de Stripe, RevenueCat, etc.)
.env
.env.local
.env.*.local

# Secretos Stripe para emulador de Cloud Functions (firebase emulators:start)
firebase/functions/.secret.local

# Salida copiada para Firebase Hosting (generada en deploy)
firebase/hosting_web/

# Caché local de Firebase CLI (firebase deploy / emulators)
firebase/.firebase/

# Node (Firebase Functions)
**/node_modules/

# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds
.fvm/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/extensions.json

# OS
.DS_Store
Thumbs.db

# Misc
*.log
coverage/
